DAY6: ES6+ Modern JavaScript & OOP

PART-1

1. Modern ES6 
    • Template Literals — `Hello ${name}!`
    • Destructuring — const { name, age } = user
        Array destructuring — const [first, second] = arr
        Object destructuring — const { title, body } = post
    • Spread Operator ... — copy/merge arrays and objects
    • Rest Parameters ... — collect remaining arguments
    • Optional Chaining ?. — safe property access
    • Nullish Coalescing ?? — default value operator
    • Short-circuit evaluation — && and || tricks

2. ES Modules
    • export — named exports and default exports
    • import — importing modules
    • Why modules? — code organisation, reusability

PART-2

3. Classes & OOP Basics
    a What is OOP? — Objects model real-world things
    b class keyword — blueprint for objects
    c constructor() — initialise properties
    d Methods inside classes
    e Inheritance — extends and super()
    f Encapsulation — private fields with 

#PROJECT
 Create a BankAccount class with deposit, withdraw, balance methods