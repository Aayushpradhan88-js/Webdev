DAY 4:  DOM Manipulation & Events

1. The DOM (Document Object Model)
    - What is the DOM? tree representation of the HTML page ✅
    - How JS interacts with HTML ✅
        - window, document objects
    -  Selecting elementis:  ✅
        a. getElementById() — by ID
        b. querySelector() — by CSS selector (modern & preferred)
        c. querySelectorAll() — returns NodeList

2. Manipulating the DOM
    - Reading & changing content ✅
        - innerHTML, textContent
    - Changing styles (element.style.color, classList.add/remove/toggle)

3. Events ✅
    - addEventListener() 
        - the modern way to handle events
        - Common events: click, input, submit, keydown, mouseover
        - Event object (e) 
        - what it is, e.target, e.preventDefault()

#PROJECT
Building a to-do list — add, check, delete tasks dynamically